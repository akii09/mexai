/**
 * MCP server — entry point stub.
 * Phase 4 will implement all five tools and workspace auto-detection.
 */

/**
 * Start the MCP server over stdio.
 * Wired to `mexai serve` in the CLI.
 */
export function startServer(): void {
  // Phase 4 implementation
  throw new Error('MCP server not yet implemented — coming in Phase 4.')
}
